services:
  web:
    image: nginx:1.19.10
    container_name: laiwan-web-test
    restart: unless-stopped
    ports:
      - "8089:80"
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./www:/usr/share/nginx/html:ro
